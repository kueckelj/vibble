% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vibble-space.R
\name{apply_offset}
\alias{apply_offset}
\alias{reverse_offset}
\title{Apply or reverse slice-wise offsets in a vbl2D object}
\usage{
apply_offset(vbl2D, offset_col, offset_row)

reverse_offset(vbl2D)
}
\arguments{
\item{vbl2D}{A \link[=vbl_doc_vbl2D]{2D vibble}.}

\item{offset_col, offset_row}{Absolute or relative offsets applied per slice
along the \code{col} and \code{row} axes.}
}
\value{
A modified \code{vbl2D} with updated coordinates and updated offset attributes.
}
\description{
Applies per-slice spatial offsets to the \code{col} and \code{row} coordinates of a
\code{vbl2D}, enabling staggered 2D slice arrangements; \code{reverse_offset()} undoes
all previously applied offsets.
}
\details{
Offsets are applied using zero-based slice indices such that slice \code{n} is shifted
by \code{offset * n}. Relative offsets (see \link{is_rel}()) are first converted to absolute
distances based on the current screen limits and then truncated to integer values.
Updated offsets are accumulated into the object's existing \code{offset_col} and
\code{offset_row} attributes.
}
\seealso{
\code{offset_col()}, \code{offset_row()}, \code{offset_axes()}, \code{screen_limits()}
}
