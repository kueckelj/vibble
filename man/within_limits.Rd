% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-spatial.R
\name{within_limits}
\alias{within_limits}
\title{Check whether values lie within numeric limits}
\usage{
within_limits(x, l, null_ok = FALSE)
}
\arguments{
\item{x}{A numeric vector to be tested.}

\item{l}{A numeric vector of length two specifying lower and upper limits.
Must satisfy the conditions checked by \link{is_limit}().}

\item{null_ok}{Logical. If \code{TRUE} and l is \code{NULL}, the function returns \code{TRUE}
for all elements of \code{x}.}
}
\value{
A logical vector indicating whether each element of \code{x} lies within
the specified limits.
}
\description{
Test whether numeric values fall strictly between a lower and
upper bound. Returns a logical vector with one entry per element of \code{x}.
}
\details{
If limits are provided, they must be valid according to \link{is_limit}().
The function performs comparisons with \code{>=} and \verb{=<}.
}
\examples{
# Example 1: Basic usage
x <- c(5, 10, 15, 20)
within_limits(x, l = c(9, 18))

# Example 2: Open limits when l = NULL
within_limits(x, l = NULL, null_ok = FALSE)
within_limits(x, l = NULL, null_ok = TRUE)

}
\seealso{
\link{is_limit}()
}
