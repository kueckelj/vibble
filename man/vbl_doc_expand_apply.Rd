% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-ggvibble.R
\name{vbl_doc_expand_apply}
\alias{vbl_doc_expand_apply}
\alias{.apply_expand}
\alias{.apply_expand_lgl}
\alias{.apply_expand_abs}
\alias{.apply_expand_rel}
\title{Internal expand helpers}
\usage{
.apply_expand(limit, expand)

.apply_expand_lgl(limit, expand)

.apply_expand_abs(limit, expand)

.apply_expand_rel(limit, expand)
}
\arguments{
\item{limit}{Numeric vector of length two giving the current axis limits.}

\item{expand}{Expand specification, validated by \code{is_expand()}.
Interpreted as absolute, relative, or logical depending on
\code{is_expand_abs()}, \code{is_expand_rel()}, and \code{is_expand_lgl()}.}
}
\value{
A numeric vector of length two containing the updated limits.
\code{.apply_expand()} dispatches to one of the specialized helpers.
}
\description{
Internal utilities to expand a 2D limit vector according to absolute, relative,
or logical expand rules. These functions form the low-level backend used by
higher-level helpers such as \link{expand_lim2D}().
}
\details{
Expansion rules.
\itemize{
\item \strong{Logical} (\code{.apply_expand_lgl()}): Expands each side by a fixed fraction of the current range.
The fraction is taken from \code{vbl_opts("expand.fraction")}.
\item \strong{Absolute} (\code{.apply_expand_abs()}): Adds \code{expand} units to each side.
\item \strong{Relative} (\code{.apply_expand_rel()}): Adds \code{expand * diff(limit)} units to each side.
}

\code{.apply_expand()} acts as a simple dispatcher, selecting the correct rule based on
the type of \code{expand}.
}
\keyword{internal}
