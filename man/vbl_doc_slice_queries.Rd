% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-vibble2D.R, R/vibble-doc.R
\name{slices}
\alias{slices}
\alias{slices.vbl}
\alias{slices.vbl2D}
\alias{slices.ggvibble}
\alias{slices_cond}
\alias{slices_cond.vbl}
\alias{slices_cond.vbl2D}
\alias{slices_mid}
\alias{slices_mid.vbl}
\alias{slices_mid.vbl2D}
\alias{slices_max_area}
\alias{slices_max_area.vbl}
\alias{vbl_doc_slice_queries}
\title{Slice queries}
\usage{
slices(x, ...)

\method{slices}{vbl}(x, plane = vbl_def(), ...)

\method{slices}{vbl2D}(x, ...)

\method{slices}{ggvibble}(x, ...)

slices_cond(x, ...)

\method{slices_cond}{vbl}(x, .cond, n = vbl_def(), plane = vbl_def())

\method{slices_cond}{vbl2D}(x, .cond, n = vbl_def())

slices_mid(x, ...)

\method{slices_mid}{vbl}(x, n = vbl_def(), radius = 0.2, plane = vbl_def())

\method{slices_mid}{vbl2D}(x, n = vbl_def(), radius = 0.2)

slices_max_area(x, ...)

\method{slices_max_area}{vbl}(x, .cond, n = vbl_def(), plane = vbl_def())
}
\arguments{
\item{x}{An object for which a method has been defined.}

\item{...}{Passed to methods.}

\item{plane}{Character scalar. The anatomical orientation, one of \emph{'sag', 'axi' or 'cor'}.
Defaults to the \link[=vbl_doc_defaults]{global default plane}.}

\item{.cond}{A logical filter expression evaluated with data-masking semantics.}

\item{n}{Integer scalar. Target number of slices to return after downsampling.}

\item{radius}{Numeric scalar controlling the slice window around the mid slice.}
}
\value{
Integer vector of slice indices.
}
\description{
Query available slice indices and select slices by condition or around the mid slice.
}
\details{
\link{slices}() returns the unique slice indices present in the input.
\link{slices_cond}() returns slices for which \code{.cond} evaluates to \code{TRUE} for at least one voxel in that slice.
\link{slices_mid}() returns a window of slices around the mid slice and optionally downsamples to \code{n}.

For \code{radius < 1}, the radius is interpreted as a fraction of the full slice range.
For \code{radius >= 1}, the radius is interpreted as a number of slices.
}
\note{
\link{slices_cond}() groups by the slice axis and then extracts the slice values from the filtered data.
}
\examples{
vbl <- example_vbl()

# all available slices in a plane
slices(vbl, plane = "axial")

# select slices where at least one voxel matches a condition
slices_cond(vbl, t1ce > 0.5, n = 12, plane = "axial")

# select slices around the mid slice
slices_mid(vbl, n = 9, plane = "axial", radius = 0.2)
}
\seealso{
\link{slices_range}(),
\link{slices_min}(),
\link{slices_mid1}(),
\link{slices_max}().
}
