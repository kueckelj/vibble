% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vibble-io-nifti.R
\name{dcm_to_vbl}
\alias{dcm_to_vbl}
\title{Convert DICOM data to a vibble}
\usage{
dcm_to_vbl(x, var, rm0 = FALSE, verbose = vbl_opts("verbose"), ...)
}
\arguments{
\item{x}{Path to a DICOM directory or vector of DICOM files.}

\item{var}{Character name of the variable under which voxel values will be
stored in the output vibble (e.g. \code{"t1"}, \code{"flair"}).}

\item{rm0}{Logical. If \code{TRUE}, remove voxels with value 0 from the resulting
vibble.}

\item{verbose}{Logical. If \code{TRUE}, informative messages are printed in the
console.}

\item{...}{Additional arguments forwarded to \link{convertDicom}().}
}
\value{
A \link[=vbl_doc_vbl]{vibble}.
}
\description{
Convert a DICOM directory or file set into a NIfTI volume and return the result as a \link[=vbl_doc_vbl]{vibble}.
}
\details{
The function performs:
\enumerate{
\item Conversion of the provided DICOM input into a temporary NIfTI file via \link{convertDicom}().
\item Import of that NIfTI volume using \link{nifti_to_vbl}().
\item Automatic cleanup of the temporary NIfTI file.
}

All spatial metadata (orientation, dimensions, spacing) follow the
conventions defined in the vibble 3D spatial reference system.
}
\examples{
\dontrun{
d <- dcm_to_vbl("path/to/dicom_folder", var = "t1")
}

}
\seealso{
\link{nifti_to_vbl}
}
