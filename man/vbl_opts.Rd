% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vibble-options.R
\name{vbl_opts}
\alias{vbl_opts}
\title{Manage vibble options}
\usage{
vbl_opts(...)
}
\arguments{
\item{...}{Named arguments to set options or a single unnamed character to retrieve a specific option.}
}
\value{
Returns a named list of current vibble options when called without arguments,
the value of one option when called with a single character input, or invisibly returns
previous values when setting options.
}
\description{
Sets or retrieves global options that control the behaviour of vibble functions.
}
\details{
Options are stored using standard R options with the prefix \code{vibble.}.
\code{vbl_opts()} behaves differently depending on how it is called.

\itemize{
\item \code{vbl_opts()} returns all current \verb{vibble.*} options.
\item \code{vbl_opts("verbose")} retrieves the value of \code{vibble.verbose}.
\item \code{vbl_opts(verbose = FALSE, ask = TRUE)} sets options and returns old values invisibly.
}
}
\note{
Options can also be set persistently via \code{.Rprofile} using \code{options(vibble.verbose = FALSE)}.
}
\examples{
vbl_opts()
vbl_opts("verbose")
vbl_opts(verbose = FALSE)

}
