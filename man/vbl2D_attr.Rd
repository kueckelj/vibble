% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-vibble2D.R, R/vibble-tests.R
\name{vbl2D_attr}
\alias{vbl2D_attr}
\alias{offset_attr}
\alias{offset_dir}
\alias{offset_dir<-}
\alias{offset_dist}
\alias{offset_dist<-}
\alias{plane}
\alias{plane<-}
\alias{is_offset}
\title{vbl2D attributes}
\usage{
offset_attr(vbl2D, which)

offset_dir(vbl2D)

offset_dir(vbl2D) <- value

offset_dist(vbl2D)

offset_dist(vbl2D) <- value

plane(vbl2D)

plane(vbl2D) <- value

is_offset(x)
}
\arguments{
\item{which}{Character scalar for \code{offset_attr()} selecting the offset attribute.
Valid values are \code{c("dir", "dist")}.}

\item{value}{New value assigned in the corresponding replacement functions.}
}
\value{
For accessor functions, the requested attribute value.
For replacement functions, the modified \code{vbl2D} object.
}
\description{
Access and modify core attributes of a \code{vbl2D} object.
These helpers provide a stable interface to metadata used for 2D visualization and offset handling.
}
\details{
The following attributes are attached to \code{vbl2D} objects and can be accessed or modified via these helpers.
They are used by plotting and slicing functions to control display, offsets, and orientation in 2D space.

The \code{lim} attribute is a list with optional entries \code{col} and \code{row}.
Each entry is either \code{NULL} or a valid limit vector tested via \code{is_limit()}.
These limits can be used by higher level functions to restrict the visible extent in column and row directions.

Offset attributes are created when \code{vibble2D()} is called with \code{offset_dist > 0}.
The direction is stored in \code{offset_dir} and the distance in \code{offset_dist}.
\code{is_offset()} tests whether a \code{vbl2D} object currently has a positive offset distance.

The \code{plane} attribute stores the anatomical plane used to generate the \code{vbl2D} object.
Valid options are \code{c("axi", "cor", "sag")} depending on the implementation of \code{vibble2D()}.
}
\note{
The \code{lim} attribute affects 2D display or computational subsetting only and does not change the underlying 3D voxel space of the vibble.
Offsets and plane metadata should be kept consistent with how \code{vibble2D()} generated the object to avoid misaligned visualizations.
}
\section{Functions}{

\itemize{
\item \code{lim()}, \verb{lim<-()}: Get and set the \code{lim} attribute, a list with optional \code{col} and \code{row} limits.
\item \code{offset_attr()}: Low level accessor for offset attributes, selecting \code{offset_dir} or \code{offset_dist} via \code{which}.
\item \code{offset_dir()}, \verb{offset_dir<-()}: Get and set the offset direction.
\item \code{offset_dist()}, \verb{offset_dist<-()}: Get and set the offset distance.
\item \code{plane()}, \verb{plane<-()}: Get and set the anatomical plane of a \code{vbl2D} object.
\item \code{is_offset()}: Logical test, evaluates to \code{TRUE} if \code{offset_dist()} is greater than zero.
}
}

\seealso{
\link{vibble2D}() for constructing \code{vbl2D} objects.
\link{is_limit}() for validating limit vectors used in \code{lim}.
}
