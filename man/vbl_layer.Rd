% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ggvibble.R
\name{vbl_layer}
\alias{vbl_layer}
\title{Create a ggvibble layer object}
\usage{
vbl_layer(fun, class_add = NULL, ...)
}
\arguments{
\item{fun}{A function that takes a \code{vbl2D} object and returns one or more
ggplot2 layers (typically a list of geoms or scales).}

\item{class_add}{Character or NULL. If character, specifies additional layer
classes for (potential) specific management in the \code{build_ggplot.ggvibble()}.}
}
\value{
A \code{ggvibble_layer} object containing the supplied function. When
added to a \code{ggvibble}, the function is executed and its returned layers are
inserted into the plot.
}
\description{
Wrap a layer-construction function so it can be added to a
\code{ggvibble} object using the \code{+} operator. This is the constructor used
internally by all \verb{layer_*()} functions.
}
\details{
\code{vbl_layer()} standardizes how custom layers are attached to ggvibble plots.
The returned object is only a container; the actual layer generation happens
when \code{+.ggvibble} evaluates \code{fun(vbl2D)} during plot building.
}
\examples{
vbl_layer(function(v) ggplot2::geom_point(aes(x = col, y = row)))

}
\seealso{
\link{ggplane}(), \link{layer_numeric}(), \link{layer_label}()
}
\keyword{internal}
