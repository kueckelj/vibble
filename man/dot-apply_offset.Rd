% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vibble-space.R
\name{.apply_offset}
\alias{.apply_offset}
\title{Apply slice-wise offsets to a 2D vibble}
\usage{
.apply_offset(vbl2D, offset_col, offset_row)
}
\arguments{
\item{vbl2D}{A \link{vibble2D}.}
}
\value{
The input \code{vbl2D} tibble with updated \code{col} and \code{row} coordinates that include the slice-wise offsets.
All attributes of the original object are preserved where possible.
}
\description{
Shift \code{col} and \code{row} coordinates of a \code{vbl2D} object by slice-specific offsets.
This is used to arrange multiple slices next to each other for 2D plotting.
}
\details{
The function computes a slice index \code{idx} based on the unique values of \code{slice} and the requested \code{offset_dir}.
Coordinates are shifted by calling \link{comp_offset_col}() and \link{comp_offset_row}() inside grouped \code{dplyr} operations so that offsets are consistent within plane, sequence, slice, and mask combinations.
}
\seealso{
\link{vibble2D}(), \link{comp_offset_col}(), \link{comp_offset_row}()
}
